/**
 * The recorder's provenance string — one declaration, stamped into every event envelope and telemetry ping. The recorder (index.js) and the ping composers (sink.js) both read it here.
 */
export const RECORDER_VERSION = "locus-recorder/0.5.7";
